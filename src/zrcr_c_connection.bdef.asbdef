projection;
strict ( 2 );
use draft;

define behavior for ZRCR_C_CONNECTION alias NameConnection
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}